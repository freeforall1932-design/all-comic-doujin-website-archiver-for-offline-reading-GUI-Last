# Deprecated files

This directory retains files from the older source snapshot that are not byte-for-byte duplicates of the latest source now located at the repository root. They are archived for reference and are not part of the current source tree.

- `src/extractor/extractor_001.py` through `extractor_072.py` correspond to older revisions of current numbered extractor modules.
- `src/extractor/extractor_073.py` through `extractor_075.py` existed only in the older snapshot.
- `translation/` contains older revisions of translation resources that differed from the latest snapshot.
- `repository-readme.md` and `legacy-note.txt` preserve the prior repository-level context.
